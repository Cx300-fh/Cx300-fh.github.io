import { ArrowUpRight } from 'lucide-react';
import { PageShell, Tags } from '@/components/site';
import { posts } from '@/lib/posts';
export const metadata = {title:'Blog — Yuxin Li',description:'A space for future research notes and personal writing.'};
export default function Blog() { return <PageShell active="Blog" eyebrow="WRITING / NOTES & THOUGHTS" title="Blog" description="Research notes, things learned, and occasional thoughts.">{posts.length ? <div className="blog-grid">{posts.map(p=><article className="content-card" key={p.slug}><a className="card-cover note-cover" href={p.href}><img src={p.image} alt={p.imageAlt} loading="lazy"/><span className="image-badge">{p.category}</span></a><div className="card-body"><p className="paper-meta">{p.date} · {p.readingTime}</p><h2><a href={p.href}>{p.title}<ArrowUpRight size={20}/></a></h2><p>{p.description}</p><Tags items={p.tags}/><a className="text-link" href={p.href}>Read post <ArrowUpRight size={14}/></a></div></article>)}</div> : <div className="blog-empty"><span className="eyebrow">THE NOTEBOOK / 00</span><h2>A blank page,<br/><em>for now.</em></h2><p>No posts yet. More to come.</p><a className="text-link" href="/publications/">Explore my research in the meantime <ArrowUpRight size={14}/></a></div>}</PageShell>; }
