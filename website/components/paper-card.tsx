import { ArrowUpRight } from 'lucide-react';
import { papers } from '@/lib/content';
import { Tags } from './site';
export function PaperCard({paper:p}:{paper:typeof papers[number]}) { return <article className="paper" id={p.slug}><a className={`paper-visual ${p.slug}`} href={p.project} target="_blank" rel="noreferrer" aria-label={`${p.name} project page`}><img src={`/images/${p.image}`} alt={p.alt} loading="lazy"/><span className="image-badge">PREPRINT · 2026</span></a><div className="paper-copy"><p className="paper-meta">{p.month}</p><h3><a href={p.paper} target="_blank" rel="noreferrer">{p.name}<ArrowUpRight/></a></h3><p className="paper-subtitle">{p.subtitle}</p><p className="authors">{p.authors.split('Yuxin Li')[0]}<strong>Yuxin Li</strong>{p.authors.split('Yuxin Li')[1]}</p><p className="paper-description">{p.description}</p><Tags items={p.tags}/><div className="paper-links"><a href={p.paper} target="_blank" rel="noreferrer">arXiv <ArrowUpRight size={14}/></a><a href={p.project} target="_blank" rel="noreferrer">Project <ArrowUpRight size={14}/></a>{p.code&&<a href={p.code} target="_blank" rel="noreferrer">Code <ArrowUpRight size={14}/></a>}</div></div></article>; }
